{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "command": "make clean && make",
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}/jsonpath-lib"
      }
    }
  ]
}